package com.dbaagent.repository;

import com.dbaagent.model.PrivateBetaRequest;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PrivateBetaRequestRepository extends JpaRepository<PrivateBetaRequest, Long> {
}
